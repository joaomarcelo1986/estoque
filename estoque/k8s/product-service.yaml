apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: estoque-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
      - name: product
        image: <OCIR_REPO>/product-service:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: estoque-app
spec:
  selector:
    app: product-service
  ports:
  - port: 80
    targetPort: 5000
